[project]
name = "dashpi"
version = "1.0.0"
description = "The dashboard for pi"
dependencies = [
    "flask",
    "flask-login",
    "psutil"
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"